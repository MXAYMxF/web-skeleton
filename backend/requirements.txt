# Core
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1
psycopg2-binary>=2.9.9
redis>=5.0.1
asyncpg>=0.29.0

# WebSockets
websockets>=12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.5
pytest-cov>=4.1.0
httpx>=0.26.0
Faker>=22.6.0

# Development
black>=24.1.0
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0

# Documentation
mkdocs>=1.5.3
mkdocs-material>=9.5.3

# Web3
web3>=6.14.0

# AI / LLM providers (OPTIONAL — imported lazily by app.core.ai adapters).
# Only needed when AI_PROVIDER is set to that provider AND its key is configured.
# The default AI_PROVIDER=mock needs neither: the app imports and the test suite
# runs green without these installed. Deliberately kept out of setup.py
# install_requires so `pip install -e .` / tests stay lean.
anthropic>=0.40.0   # verify latest at install time
openai>=1.40.0      # verify latest at install time
