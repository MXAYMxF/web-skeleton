# Backend API base URL.
# The app displays this in the footer (API status indicator) and next.config
# proxies all /api/* requests to it. Change it here — not in any .ts/.js file.
NEXT_PUBLIC_API_URL=http://localhost:8000
